# Data
data/raw/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# MLflow
mlruns/
mlartifacts/

# Model checkpoints: deliberately NOT ignored. The 5 trained checkpoints
# total ~1MB (small models: feature_dim=165, hidden_dim=64) and carry no
# license restriction (unlike data/raw/, which stays ignored below) -- they
# are committed so a fresh clone + `docker build` gets a working demo
# without requiring a multi-hour retrain first. See README/run.sh.

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Env files
.env
data/processed/*.pkl
full_run_log.txt

# Personal project prep notes (decisions log + end-to-end explainer), not for the repo
Prep/
